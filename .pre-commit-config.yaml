repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Keep in lockstep with the pin in .github/workflows/tests.yml.
    rev: v0.15.16
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
